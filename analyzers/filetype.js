const fs = require('fs');
const path = require('path');

let fileT = function (x) {
  if (path.extname(x)=='.docx') {
    //write file type to txt file
  } else if (path.extname(x)=='.pdf') {
     //write file type to txt file
  }
}